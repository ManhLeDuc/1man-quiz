import React, { useState } from 'react';
import './style.css'

const Answer = () => {
  return (
    <div
      className="answer"
      onClick={() => { }}
    >
      {"hello"}
    </div>
    // <button type="button" className="answer">Primary</button>
  );
};

export default Answer;
